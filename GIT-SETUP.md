# 🔧 Git Setup Guide

## Текущий статус

✅ **Git инициализирован** - проект уже находится под контролем версий
⏳ **Не подключен к GitHub** - удаленный репозиторий не настроен

---

## 📋 Что нужно сделать

### 1. Создать репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `nextseed` (или любое другое)
4. Описание: "Educational platform for young entrepreneurs"
5. **НЕ** создавайте README, .gitignore или license (они уже есть)
6. Нажмите "Create repository"

### 2. Подключить локальный проект к GitHub

Скопируйте URL вашего репозитория (например: `https://github.com/username/nextseed.git`)

Затем выполните в терминале:

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/YOUR-USERNAME/nextseed.git

# Проверить, что добавлено
git remote -v
```

### 3. Закоммитить текущие изменения

```bash
# Добавить все файлы
git add .

# Создать коммит
git commit -m "feat: implement student platform with projects, competitions, and outreach"

# Отправить на GitHub
git push -u origin main
```

Если возникнет ошибка про ветку `master` vs `main`:

```bash
# Переименовать ветку в main
git branch -M main

# Затем снова push
git push -u origin main
```

---

## 🔄 Регулярные коммиты

После внесения изменений:

```bash
# Посмотреть статус
git status

# Добавить измененные файлы
git add .

# Создать коммит с описанием
git commit -m "описание изменений"

# Отправить на GitHub
git push
```

---

## 📝 Рекомендуемые сообщения коммитов

Используйте префиксы для ясности:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование, стили
- `refactor:` - рефакторинг кода
- `test:` - тесты
- `chore:` - обновление зависимостей, конфигурации

Примеры:
```bash
git commit -m "feat: add project creation form"
git commit -m "fix: resolve modal closing issue"
git commit -m "docs: update README with API documentation"
```

---

## 🚨 Важно

### Файлы, которые НЕ должны попасть в Git:

Уже настроено в `.gitignore`:
- `node_modules/` - зависимости
- `.env` - секретные ключи
- `.next/` - build файлы
- `*.log` - логи

### Перед первым push проверьте:

```bash
# Убедитесь, что .env НЕ в списке
git status

# Если .env там есть, удалите его из индекса
git rm --cached .env
git commit -m "chore: remove .env from git"
```

---

## 🔐 SSH ключ (опционально, но рекомендуется)

Для удобной работы без ввода пароля:

1. Генерация SSH ключа:
```bash
ssh-keygen -t ed25519 -C "your-email@example.com"
```

2. Добавление в GitHub:
   - Скопируйте содержимое `~/.ssh/id_ed25519.pub`
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте ключ

3. Изменить URL репозитория:
```bash
git remote set-url origin git@github.com:YOUR-USERNAME/nextseed.git
```

---

## ✅ Проверка подключения

```bash
# Проверить удаленный репозиторий
git remote -v

# Должно показать:
# origin  https://github.com/YOUR-USERNAME/nextseed.git (fetch)
# origin  https://github.com/YOUR-USERNAME/nextseed.git (push)
```

---

## 📊 Текущие изменения

Файлы, готовые к коммиту:
- ✅ Новые компоненты студенческой платформы
- ✅ Обновленная главная страница
- ✅ Улучшенные UI компоненты
- ✅ Обновленная схема базы данных
- ✅ Документация (README, STATUS)

Всего: **~50+ файлов** готовы к первому коммиту!

---

**Готово к работе! 🚀**
